version: '3.8'

services:
  app:
    container_name: spring_app_demo
    image: spring_app_demo
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8889:8082"
    networks:
      - demo-app_default
    depends_on:
      - db
  db:
    container_name: postgres
    image: postgres:16.10
    build:
      context: .
      dockerfile: DockerfileDB
    environment:
      POSTGRES_DB: mydb
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: 123456
    ports:
      - "5433:5432"
    networks:
      - demo-app_default
    volumes:
      - db_data:/var/lib/postgresql/data

networks:
  demo-app_default:
    driver: bridge

volumes:
  db_data: